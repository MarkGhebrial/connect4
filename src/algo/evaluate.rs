use crate::connect4::board::Board;

/// Given a board position, approximate which player has the advantage and by how much. Positive
/// evaluations mean yellow has an advantage and negative evaluations mean red has an advantage.
pub fn evaluate(board: &Board) -> i32 {
    todo!()
}
